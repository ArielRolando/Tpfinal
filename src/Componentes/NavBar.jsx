import React from 'react';
import CardWidget from './CardWidget';

const NavBar = () => {
    return (
        <nav>
            {/* Otros elementos de la barra de navegación, como enlaces a diferentes secciones */}
            <CardWidget />
        </nav>
    );
};

export default NavBar;
